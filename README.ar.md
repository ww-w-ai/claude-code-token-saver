# cc-token-saver

> **Claude Code يقطعك باستمرار؟ ليس بعد الآن.**
>
> أنفق أقل، برمج لفترة أطول، واعرف بالضبط أين تذهب token الخاصة بك — بدون أي إعداد.

كيف؟ إدارة context تلقائية، تتبع تكاليف لحظي، وتحكم بالجلسة مع مراعاة cache — كل ذلك في إضافة واحدة.

---

## 😤 المشكلة: $200 شهريا ولا تستطيع إنجاز عملك

خطة Claude Code Max ($200/شهر). يفترض أنها كافية. لكنها ليست كذلك.

**rate limit على نافذة 5 ساعات متدحرجة.** أنت في عمق كتابة الكود وفجأة يتوقف. لا مؤقت. لا وقت متوقع. فقط انتظر.

**انتهاء صلاحية cache.** تعود من استراحة الغداء. مضت أكثر من ساعة. ترسل prompt واحدا فيُعاد إرسال 900K token بالسعر الكامل. التكلفة؟ $9 في طلقة واحدة.

**تكاليف خفية.** لا توجد طريقة لمعرفة كم تنفق في الوقت الفعلي. لا تكتشف ذلك إلا بعد أن يضرب rate limit.

**كل شيء يدوي.** حجم context، توقيت انتهاء cache، تفويض SubTask، تنظيف session. لا أحد يستطيع تتبع كل هذا أثناء البرمجة.

cc-token-saver يتولى كل ذلك تلقائيا. **ثبّته مرة واحدة. انتهى.**

---

## 🚀 التثبيت

```
claude plugin marketplace add ww-w-ai/cc-token-saver
claude plugin install cc-token-saver
```

يعمل تلقائيا بعد التثبيت. بدون أي إعداد. يتطلب [Claude Code](https://claude.ai/claude-code) v2.1.71+.

للمراقبة الحية:

```
/setup-statusline install
```

---

## 🛡️ الميزة 1: Token Guardian

**يكتشف انتهاء صلاحية cache ويمنع تلقائيا إعادة الإرسال المكلفة.**

مدة صلاحية prompt cache في Claude Code هي ساعة واحدة. ابتعد لأكثر من ساعة وينتهي cache. رسالتك التالية تعيد إرسال كامل context بالسعر الكامل. عند 900K token، هذا $9 في طلقة واحدة.

Token Guardian يتتبع وقت آخر استجابة. إذا مرت أكثر من 3,590 ثانية (TTL ناقص 10 ثوانٍ احتياطية)، يمنع prompt ويعرض تحذيرا.

```
🚨 انتهت صلاحية الذاكرة المؤقتة (68د 23ث خمول)

انتهت صلاحية الذاكرة المؤقتة. المتابعة ستعيد إرسال كل السياق.
قد تزداد التكلفة بشكل كبير.

👉 /context — تحقق من استخدام السياق الحالي قبل اتخاذ القرار
👉 /clear → /continue — إعادة التعيين ثم استعادة السياق السابق (موصى به، أقل تكلفة)
👉 إعادة الإرسال — المتابعة كما هو (تكلفة إعادة التخزين المؤقت الكاملة تُتكبّد)
```

فقط أعد إرسال نفس prompt بعد التحذير — سيمر. التحذير يظهر مرة واحدة فقط لكل فترة خمول، فلا يزعجك أبدا. رسائل التحذير تُعرض بـ 23 لغة بناء على لغة نظام التشغيل.

**النتيجة:** تكاليف إعادة cache المكلفة تُمنع تلقائيا. لا يتطلب أي جهد.

---

## 🧠 الميزة 2: Smart Session Architecture

**ثبّته وستبدأ أنماط العمل المُحسّنة للتكلفة تلقائيا.**

معظم المستخدمين يفعلون كل شيء في session الرئيسية. قراءة الملفات، توليد الكود، تشغيل الاختبارات. كل مخرجات تتراكم في context وتُعاد إرسالها مع كل رسالة. الجلسة تتضخم. التكاليف تتصاعد.

Session Architect يحقن تلقائيا استراتيجية تفويض عند بداية كل session.

|                  | Main Session                      | SubTask                               |
| ---------------- | --------------------------------- | ------------------------------------- |
| الدور             | التصميم، القرارات، المراجعة         | التنفيذ، توليد الكود، ملفات متعددة  |
| مستوى cache       | ساعة واحدة (ephemeral_1h)             | 5 دقائق                                  |
| تكلفة كتابة cache | ＄10/MTok                          | ＄6.25/MTok                            |
| حجم context     | ~94K متوسط                          | ~33K متوسط                              |

SubTask لديها **كتابة cache أرخص بـ 37.5%** من Main. حجم context أيضا أصغر بكثير. تفويض العمل الثقيل إلى SubTask يخفض التكاليف بشكل كبير.

**النتيجة:** Claude يعمل تلقائيا بنمط موفر للتكلفة. لا تحتاج للتفكير في ذلك.

---

## 🪶 وضع الإيجاز

**نفس المحتوى. حشو أقل. مفعّل افتراضياً.**

نفس خطاف SessionStart يُضمّن أيضاً قاعدة لأسلوب الاستجابة تعمل في **كل جلسة وكل نموذج** — بدون أعلام، بدون إعداد. ثلاثة أشياء تتغير:

- **حذف المقدمة** — لا "دعني أتحقق…"، ولا "سأفعل الآن…"، ولا إعادة صياغة سؤالك، ولا تلخيص ما يُظهره diff بالفعل
- **التنسيق المناسب للمحتوى** — نقاط للقوائم، نثر للاستدلال (المقايضات، السببية، المبررات). لا فرض لأي منهما
- **تعبير أكثر إحكاماً** — نفس النقطة، كلمات أقل. النثر الأوضح هو النثر الأقصر

الحد الصارم: لا تُسقط محتوى، ولا تتخطى التحقق، ولا تطمس الفروق الدقيقة في جملة واحدة. الجوهر يبقى كاملاً؛ فقط الغلاف يتقلص.

ثبّت مرة واحدة، يُطبَّق في كل مكان.

---


## 🔄 الميزة 3: /continue — استعادة Context

**يحل محل `/compact`. صفر استدعاءات LLM. صفر تكلفة token.**

`/compact` يرسل كامل context (~1M token) إلى LLM لضغطه في ملخص 3.3%. إذا انتهى cache، هذا وحده يُطلق إعادة cache كاملة. فقدان المعلومات أمر حتمي.

`/continue` يتبع نهجا مختلفا تماما. يعالج نص session السابقة مسبقا ويحمّله مباشرة. لا استدعاء LLM. لا تكلفة. المحادثة الأصلية تُستعاد كما هي.

|                         | /compact                          | /continue                        |
| ----------------------- | --------------------------------- | -------------------------------- |
| طريقة العمل            | يرسل كامل context إلى LLM للتلخيص | يعالج النص مسبقا، يقرأ مباشرة |
| استدعاءات LLM               | مطلوبة (عادة 100K+ token) | 0                                |
| تكلفة token              | عالية                              | 0                                |
| فقدان معلومات        | نعم (ملخص 3.3%)                | لا (الأصل محفوظ)        |
| سرعة المعالجة        | عشرات الثواني                   | < 1 ثانية (حتى ملفات 60MB+)       |
| عند انتهاء cache   | تكلفة إعادة cache كاملة فوقها         | لا تأثير                        |
| استعادة session متعددة   | غير ممكنة                      | مدعومة                        |

الاستخدام: `/clear` ثم `/continue`. ستظهر قائمة session السابقة. اختر واحدة للاستعادة. للاسترداد السريع: `/continue last`.

**النتيجة:** استأنف عملك السابق بتكلفة صفرية. بدون فقدان معلومات.

---

## 📊 الميزة 4: شريط الحالة الحي

**مراقبة token والتكلفة في الوقت الفعلي. أقل من 50ms حمل إضافي.**

شغّل `/setup-statusline install` مرة واحدة وسيظهر شريط حالة دائم أسفل Claude Code.

```
[RUN🟢] $0.10/$12.23 | [5H🟢] 9% ⏳1h32m | [CTX🟢] 22%
```

| المؤشر        | ما يعرضه                       | 🟢 عادي | 🟡 تحذير | 🔴 حرج |
| ---------------- | ----------------------------------- | --------- | ---------- | ----------- |
| RUN (فرق)      | تكلفة آخر استدعاء API           | < ＄0.30   | >= ＄0.30   | >= ＄1.00    |
| RUN (تراكمي) | التكلفة التراكمية لهذا المجلد     | —         | —          | —           |
| 5H               | استخدام نافذة 5 ساعات + عداد تنازلي | < 70%     | >= 70%     | >= 90%      |
| CTX              | استخدام نافذة context                | < 35%     | >= 35%     | >= 70%      |

عندما يصل أي مؤشر إلى مستوى تحذير أو حرج، يظهر تلميح `→ /usage-view current` تلقائيا.

للإزالة: `/setup-statusline uninstall` (الإعدادات السابقة تُستعاد تلقائيا).

**النتيجة:** راقب حالة التكلفة بنظرة واحدة. تصرّف قبل فوات الأوان.

---

## 📈 لوحة الاستخدام (/usage-view)

**أخيرا أجب عن: "لماذا ضربني rate limit؟"**

حتى الآن، الوصول إلى rate limit كان يجعلك غاضبا فقط. لا طريقة لمعرفة السبب. أي session استهلكت أكثر token؟ متى ارتفعت التكاليف؟ ما الأنماط في استخدامك؟ كل شيء مخفي.

`/usage-view` يعرض كل شيء. لوحة HTML تفاعلية تفتح في المتصفح، تتيح لك تحليل أنماط الاستخدام وتتبع مصدر ارتفاع التكاليف. لا تبعيات خارجية. يعمل مستقلا. قابل للمشاركة كملف.

ما تتضمنه:

- اتجاهات التكلفة يوميا / ساعيا / حسب يوم الأسبوع — اكتشف متى تستهلك أكثر token
- تفصيل token (إدخال، إخراج، كتابة cache، قراءة cache) — اعرف ما يرفع التكاليف
- تحليل التكلفة لكل session — حدد المهام المكلفة
- خط زمني لنافذة 5 ساعات (مشتركو Max Plan) — تتبع محفزات rate limit
- تحليل ذكي بالـ AI — يفسر البيانات ويقترح تحسينات
- 23 لغة مدعومة (RTL مشمول؛ الرسوم البيانية/الجداول تبقى LTR)

```
/usage-view                  # كل الأوقات، كل المشاريع
/usage-view current          # نافذة 5 ساعات الحالية فقط
/usage-view last 7 days      # آخر 7 أيام
/usage-view locale ar        # العربية
```

---

## 🔬 بحث Rate Limit (/report-limit)

**مشروع مجتمعي للهندسة العكسية لصيغة rate limit.**

Anthropic لا تنشر الصيغة الدقيقة لنافذة 5 ساعات. لنكتشفها معا.

عند وصولك إلى rate limit، شغّل `/report-limit`. بيانات استخدامك الحالية تُرسل تلقائيا كـ GitHub Discussion. كلما جمعنا بيانات أكثر، اتضحت الصيغة أكثر.

---

## ✂️ الميزة 5: /setup-git-lite — تقليص تعليمات Git المدمجة في CC

**2,200 token مخفية تُستنزف من كل جلسة دون أن تعلم.**

### الاكتشاف

في 2026-04-12، كشفت [مشكلة على GitHub](https://github.com/anthropics/claude-code/issues/47107) أن إعداد `includeGitInstructions` المدمج في Claude Code يحرق token في صمت مع كل جلسة. أثبت إعادة الإنتاج المستقلة عبر [هذا الـ gist (spilist)](https://gist.github.com/spilist/b0db92a859192f5ec6199d3f35a81b98) الأرقام: **+6,031 token في كتابة cache_create** لكل جلسة بعد كل git commit، **+1,690 token في cache_read** في كل استدعاء API.

### تحليل مصدر CC — أين تذهب الـ token

تتبعنا الـ token إلى نقطتَي حقن مستقلتين في مصدر Claude Code (v2.1.88):

**1. لقطة `gitStatus` (~500 tok) — system prompt**
- `context.ts:36-111` تجمع `getGitStatus()` الفرع + الفرع الرئيسي + user.name + حالة كاملة (حتى 2000 محرف) + **آخر 5 commits**
- تُدمج وتُلحق بـ system prompt عبر `appendSystemContext` (`utils/api.ts:437`)
- كل commit جديد، وكل ملف معدّل، وكل تغيير فرع يغيّر النص → إلغاء صلاحية prefix cache

**2. تعليمات سير عمل Commit/PR (~1,700 tok) — وصف أداة Bash**
- `tools/BashTool/prompt.ts:53` تلحق أكثر من 60 سطرا من بروتوكول السلامة وخطوات Commit التفصيلية وأمثلة HEREDOC وقوالب إنشاء PR بوصف أداة `Bash`
- مخزَّنة مؤقتا جانب system prompt، لكنها ترسَل كمعامل `tools[]`

### لماذا هذا مكلف

بنية cache (`utils/api.ts:321` `splitSysPromptPrefix`) لها ثلاثة مسارات بحسب وجود MCP tools نشطة:

- **Path A** (MCP نشط — معظم المستخدمين): `gitStatus` داخل كتلة `cacheScope: 'org'`. أي تغيير → إعادة كتابة cache للكتلة كلها عند بداية الجلسة التالية → 6K tok `cache_create` miss.
- **Path B** (بدون MCP): `gitStatus` تذهب إلى كتلة ديناميكية `cacheScope: null`، أي تُرسَل كـ `input_tokens` جديدة في كل استدعاء API — لا cache miss، لكن لا توفير cache أيضا.
- **Path C** (مزود خارجي / betas تجريبية معطّلة): مثل Path A.

في الجلسات التفاعلية النموذجية، تعليمات Commit/PR (1.7K tok) تتراكم **في كل استدعاء API** عبر `cache_read`. على مدار 100 استدعاء بتسعير Opus 4.7، هذا ما يقارب **$0.08 لكل جلسة** فقط مقابل تعليمات يغطيها تدريب Claude أصلا في معظمه.

### كيف يتعامل cc-token-saver مع ذلك

`/setup-git-lite` يعطّل المسار الأصلي ويحقن **بديلا مضغوطا من 280 token** عبر SessionStart hook. احتفظنا بالأشياء التي تتجاوز السلوك الافتراضي لـ Claude (قواعد السلامة) فقط، وحذفنا كل ما يعرفه Claude من التدريب (سير العمل خطوة بخطوة، قوالب PR، أنماط استخدام gh).

**مُحتَفظ به — 11 قاعدة تجاوز حرجة** (القواعد التي تحوّل helpfulness الافتراضية لـ Claude إلى حذر):
- لا commit/push/amend/PR/tag/merge بدون طلب صريح من المستخدم
- لا تخطّي hooks، ولا force-push إلى main/master، ولا عمليات تدميرية، ولا تعديل git config
- لا commit لملفات تطابق `.env`، `credentials`، `*.pem`، `secret.*`
- تجنّب `git add -A` / `git add .`
- HEREDOC لرسائل commit متعددة الأسطر + تذييل `Co-Authored-By: Claude`
- لا استخدام flags تفاعلية (-i)، ولا commits فارغة
- إذا فشل pre-commit hook → أنشئ commit جديدا (وليس `--amend`)

**محذوف** — سير عمل commit خطوة بخطوة (3 خطوات)، سير عمل PR خطوة بخطوة (3 خطوات)، قالب عنوان PR ومحتواه، مراجع أوامر `gh`، تحذير flag `-uall`، تحذير `--no-edit` مع rebase، قيد `NEVER use TodoWrite or Agent tools during commit`. هذه verbosity سير عمل يؤلفها Claude بشكل صحيح من التدريب وحده.

**مُضاف** — سطر حالة git مضغوط: الفرع + HEAD short-sha + الموضوع + الحالة الحالية (حتى 20 ملف معدّل وإلا عدد). لا قائمة commits حديثة (يمكن لـ Claude تشغيل `git log` عند الطلب).

### التوفيرات المتوقعة (تسعير Opus 4.7، $25/MTok مخرجات، $5/MTok مدخلات، $0.50/MTok قراءة cache)

| العنصر | الأصلي | مع setup-git-lite | الموفَّر |
| ------ | ------- | ----------------- | -------- |
| تحميل system prompt (لكل جلسة جديدة) | ~2,200 tok cache_create | ~280 tok cache_create | ~1,920 tok |
| الاستدعاءات المتكررة في نفس الجلسة | ~1,700 tok cache_read/استدعاء | ~280 tok cache_read/استدعاء | ~1,420 tok/استدعاء |
| جلسة 100 استدعاء (Opus 4.7) | — | — | **توفير ~$0.11** |
| 20 جلسة/يوم × 22 يوم عمل | — | — | **توفير ~$48/شهر** |

### الاستخدام

```bash
/setup-git-lite status     # تشخيص للقراءة فقط — الحالة الحالية + ما سيتغير
/setup-git-lite install    # تعطيل CC native + تفعيل hook المصغّر الخاص بنا
/setup-git-lite revert     # استعادة الإعداد الافتراضي (عدواني؛ انظر أدناه)
/setup-git-lite dismiss-banner    # إخفاء نصيحة التوصية العرضية
/setup-git-lite undismiss-banner  # إعادة تفعيل النصيحة
/setup-git-lite help       # الاستخدام الكامل
```

### دلالات install

يُعدّل `install` **مكانَين** للمتانة:

1. `~/.claude/settings.json` — يضيف `"includeGitInstructions": false`
2. ملف Shell profile (`~/.zshrc`، `~/.bashrc`، إلخ) — يلحق كتلة marker تُصدّر `CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS=1`

أي منهما وحده كافٍ لتعطيل CC native؛ نضبط الاثنين لمنع environment override من إعادة تفعيل السلوك الأصلي عن طريق الخطأ. تغيير Shell يسري في Shells الجديدة فقط.

### دلالات revert — عدواني

`revert` **يحذف جميع صادرات `CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS` من ملف Shell profile**، بما فيها أي صادرات أضفتها يدويا قبل تثبيت هذه المهارة. هذا مقصود — لقد شغّلت `revert`، فنحن نستعيد الإعداد الافتراضي النظيف. ننشئ دائما نسخة احتياطية مؤرّخة من ملف Shell profile أولا.

إذا كنت تحتاج متغيّر البيئة لأسباب غير مرتبطة، دوّنه قبل تشغيل `revert` وأعد إضافته بعده.

### قبل إلغاء تثبيت cc-token-saver

**شغّل `/setup-git-lite revert` أولا**، وإلا ستبقى مع `includeGitInstructions: false` في settings.json دون hook بديل (لن يتلقّى Claude أي توجيه git على الإطلاق). لا يملك Claude Code حاليا lifecycle hook لإلغاء التثبيت، لذا لا يمكننا أتمتة هذا.

### المقايضات

ما ستفقده (ولماذا هذا عادةً مقبول):
- لن يتلقّى Claude بعد الآن `git status` / `git log -n 5` محسوبا مسبقا عند بداية الجلسة. إذا سألت "ما الذي تغيّر؟" في جلسة جديدة، سيشغّل Claude تلك الأوامر بنفسه (استدعاء أداة إضافي واحد، ~300 tok).
- لن يرى Claude بعد الآن إجراء Commit المكوّن من 3 خطوات الخاص بـ CC. في اختباراتنا عبر مئات من سير عمل Commit، معرفة مستوى التدريب تتعامل مع الحالات الحرجة (تنسيق HEREDOC، لا `--amend`، لا force-push) لأننا نحتفظ بتلك كقواعد صريحة.
- قالب PR body (‏`## Summary` + `## Test plan`) لا يُحقَن. إذا كنت تهتم بذلك التنسيق تحديدا، ضعه في CLAUDE.md الخاص بمشروعك.

### لافتة التوصية

عندما تكون تعليمات Git الأصلية لـ CC نشطة على جهازك، يعرض cc-token-saver نصيحة من فقرة واحدة عند بداية الجلسة **~20% من الوقت** (بالإضافة إلى مخرجات `/usage-view` و`/report-limit`). أخفها بشكل دائم بـ `/setup-git-lite dismiss-banner`.

---

## 💡 كيف يعمل Cache فعلا

Claude Code يرسل كامل تاريخ المحادثة إلى النموذج في كل استدعاء API. "استدعاء API" لا يعني "رسالة واحدة كتبتها." prompt واحد يُطلق استدعاءات أدوات داخلية — Grep، Read، Edit، Write — وكل واحدة استدعاء API منفصل. prompt واحد يمكن أن يسبب 10+ استدعاءات API بسهولة.

prompt cache يخفض هذه التكلفة بنسبة 90%. لكن cache له عمر محدد.

|                     | Main Session                          | SubTask                                |
| ------------------- | ------------------------------------- | -------------------------------------- |
| مدة cache           | ساعة واحدة (ephemeral_1h)                 | 5 دقائق                                  |
| كتابة cache         | ＄10/MTok                              | ＄6.25/MTok                             |
| قراءة cache          | ＄0.50/MTok                            | ＄0.50/MTok                             |
| عند انتهاء cache  | كامل context يُعاد إرساله بالسعر الكامل    | تأثير منخفض (context صغير)          |

حتى مع بقاء cache حيا، التكاليف تتراكم. إليك سيناريو متطرف لتوضيح الفرق.

### السيناريو: يوم برمجة كامل (3 ساعات صباحا → 2 ساعة غداء/اجتماع → 3 ساعات بعد الظهر)

الشروط: تسعير Opus 4، prompt واحد في الدقيقة، ~5 استدعاءات API لكل prompt (~300 استدعاء/ساعة).

#### ❌ بدون cc-token-saver

معظم العمل يحدث في Main session. حجم context ينمو بسرعة.

| المرحلة       | الوضع                         | حجم context               | التكلفة                                   |
| ----------- | --------------------------------- | -------------------------- | -------------------------------------- |
| صباحا 3س  | برمجة (غالبا في Main)           | 100K → 600K (متوسط 350K)    | 900 استدعاء × 350K × ＄0.50/M = ＄157.50  |
| غداء/اجتماع   | بعيد لمدة ساعتين                  | —                          | —                                      |
| العودة      | cache انتهت → إعادة إرسال كاملة      | 600K بالسعر الكامل            | 600K × ＄5/M + 600K × ＄10/M = ＄9       |
| العودة      | /compact (تلخيص)              | 600K → تُرسل إلى LLM        | 600K × ＄0.50/M + مخرجات الملخص = ~＄1.50 |
| بعد الظهر 3س | البرمجة تستمر (context يكبر مجددا) | 100K → 600K (متوسط 350K)   | 900 استدعاء × 350K × ＄0.50/M = ＄157.50  |
|             | الإجمالي                             |                            | ~＄326                                  |

> عند هذا المستوى من الاستخدام، ستصل غالبا إلى rate limit لنافذة 5 ساعات. **التكلفة سيئة، لكن المشكلة الحقيقية أن عملك يتوقف تماما. هذه هي اللحظة بالضبط التي ينطفئ فيها Claude Code.**

#### ✅ مع cc-token-saver

العمل الثقيل يُفوَّض إلى SubTask. Main يتولى التصميم/القرارات فقط.

| المرحلة       | الوضع                                    | حجم context                | التكلفة                               |
| ----------- | -------------------------------------------- | --------------------------- | ---------------------------------- |
| صباحا 3س  | برمجة (Main: تصميم، SubTask: تنفيذ) | Main 100K → 300K (متوسط 200K) | 900 استدعاء × 200K × ＄0.50/M = ＄90 |
| غداء/اجتماع   | بعيد لمدة ساعتين                             | —                           | —                                  |
| العودة      | ⚡ Token Guardian يمنع → /clear + /continue | —                           | ＄0 (لا استدعاءات LLM)                 |
| بعد الظهر 3س | البرمجة تستمر                             | Main 100K → 300K (متوسط 200K) | 900 استدعاء × 200K × ＄0.50/M = ＄90 |
|             | الإجمالي                                        |                             | ~＄180                              |

#### 💰 النتيجة

> **＄326 → ＄180. ＄146 وفر في اليوم (45%).**
>
> الأمر لا يتعلق بالتكلفة فقط. token أقل في نفس الوقت يعني **لن تصل إلى rate limit وتستطيع مواصلة العمل.** هذا هو الفرق الحقيقي.

### أين يتدخل cc-token-saver

```
[بدء الجلسة]
    │
    ├─ Session Architect → يحقن نمط تفويض SubTask تلقائيا
    │                       يبقي Main context تحت 250K
    │
[أثناء العمل]
    │
    ├─ Status Line → مراقبة لحظية للتكلفة/context/rate limit
    │                  تنبيه فوري عند دخول منطقة التحذير
    │
[خمول +1 ساعة]
    │
    ├─ Token Guardian → يكتشف انتهاء cache، يمنع قبل الإرسال
    │
[إعادة تشغيل الجلسة]
    │
    └─ /continue → يستعيد context السابق بتكلفة صفرية (لا استدعاءات LLM)
```

---

## 🔧 التثبيت من المصدر والتخصيص

```bash
git clone https://github.com/ww-w-ai/cc-token-saver.git
claude plugin marketplace add /path/to/cc-token-saver
claude plugin install cc-token-saver@cc-token-saver
```

cc-token-saver مفتوح المصدر بالكامل. الكود كله JavaScript عادي + سكريبتات Bash تتبع هيكل الإضافة القياسي. عدّل ما تشاء.

- **hooks/** — تغيير عتبة انتهاء cache، تخصيص رسائل التحذير، تعديل قواعد session architecture
- **scripts/** — منطق التحليل، بناء التقارير، تنسيق شريط الحالة
- **skills/** — كيف يعمل /continue و /usage-view، قوالب prompt
- **locales/** — إضافة/تعديل الترجمات، إضافة لغات جديدة
- **skills/usage-view/** — تغييرات تصميم واجهة لوحة المعلومات

اجعله ملكك. انسخ المستودع، جرّب، وأرسل PR إذا وجدت شيئا أفضل.

---

## 🌐 اللغات المدعومة

23 لغة مدعومة. تم اختيارها بمقارنة أعلى 20 دولة في استخدام Claude Code مع أعلى 20 لغة عالميا من حيث عدد المتحدثين. لغة العرض تُكتشف تلقائيا من لغة نظام التشغيل. يمكنك أيضا التحديد يدويا: `/usage-view locale ar`

|                 |                 |                |                 |
| --------------- | --------------- | -------------- | --------------- |
| 🇺🇸 English    | 🇰🇷 Korean     | 🇯🇵 Japanese  | 🇨🇳 Chinese    |
| 🇪🇸 Spanish    | 🇫🇷 French     | 🇩🇪 German    | 🇧🇷 Portuguese |
| 🇮🇹 Italian    | 🇷🇺 Russian    | 🇸🇦 Arabic    | 🇮🇳 Hindi      |
| 🇧🇩 Bengali    | 🇮🇩 Indonesian | 🇲🇾 Malay     | 🇹🇭 Thai       |
| 🇻🇳 Vietnamese | 🇹🇷 Turkish    | 🇵🇱 Polish    | 🇳🇱 Dutch      |
| 🇮🇱 Hebrew     | 🇸🇪 Swedish    | 🇳🇴 Norwegian |                 |

الترجمات الحالية مُولّدة بالذكاء الاصطناعي. مساهمات المتحدثين الأصليين مرحب بها — عدّل ملف JSON للغتك في `locales/` وأرسل PR.

---

## 💡 نصائح

### افهم cache وستعرف أين يذهب المال

- **prompt واحد ≠ استدعاء API واحد.** في كل مرة يستدعي Claude أداة Grep أو Read أو Edit، يُعاد إرسال كامل context. prompt واحد يطلق 10+ استدعاءات API بسهولة. اكتب prompt واضحا لتقليل استدعاءات الأدوات غير الضرورية وخفض التكاليف.
- **مؤقت cache يُعاد من آخر استدعاء API، وليس من آخر prompt كتبتها.** واصل العمل ولن ينتهي cache أبدا. الخطر هو الابتعاد. Token Guardian يمنع تلقائيا مرة واحدة، فعند عودتك تختار: إعادة تعيين context أو المتابعة كما هو.
- **حجم context = مضاعف التكلفة.** نفس استدعاء API عند 200K مقابل 800K يكلف 4 أضعاف. عندما يتجاوز [CTX] في شريط الحالة 35% (🟡)، هذه إشارتك لتفويض أكثر إلى SubTask.

### عادات تخفض التكاليف

- **اجعل CLAUDE.md مختصرا.** يُحمّل في system prompt مع كل استدعاء API. كل سطر يكلف مالا.
- **فوّض العمل الثقيل إلى SubTask.** توليد الكود، تعديل ملفات متعددة، تشغيل الاختبارات لا تنتمي إلى Main. SubTask لديها context أصغر ومستوى cache أرخص.
- **بعيد لأكثر من ساعة؟** `/clear` → عد → `/continue`. context يُستعاد بتكلفة $0.
- **[5H] فوق 70% (🟡)؟** أبطئ. انتقل لمهام مراجعة خفيفة أو زد تفويض SubTask لتقليل عدد استدعاءات API في Main.
- **استخدم `/btw` للأسئلة الجانبية.** لا تدخل تاريخ المحادثة، فيبقى context مختصرا.

---

## License

Apache-2.0
