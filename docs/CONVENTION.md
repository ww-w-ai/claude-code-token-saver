# Doc convention — claude-code-token-saver

> Status: LIVING. Local instance of the `cowork-doc-sync` taxonomy contract (§6). Read this every doc-sync run.

This repo is a **shipped plugin**, not a documentation project. Most markdown here is product
surface (what users and Claude read at runtime), not internal docs. The taxonomy applies to the
small remainder. Sections 1-2 say what NOT to reorganize; section 3 is the one that matters most
on a release.

## 1. Taxonomy deviations

Only two numbered folders exist, and they are created on demand:

| Folder | Content |
|---|---|
| `docs/00-reference` | Stable background that describes something outside this repo (e.g. CC's transcript format) |
| `docs/05-reports` | Dated work snapshots — doc-init/gap analysis, code health, QA runs |

Everything else in `docs/` is **not** taxonomy material:

- `docs/images/` — screenshots referenced by **all 23 README locales**. Moving one breaks 23 files. Assets, not docs.
- `docs/commit-log/` — generated by `/cowork-commit`. Outside the numbering by contract (§4). Keep, never absorb.

Do not scaffold `01-built`, `02-planned`, `03-manual`, `04-legacy`, `06-research`, `99-misc`
speculatively. Create one when it has real content.

## 2. Files that look like docs but are product — never relocate

| Path | Why it must stay |
|---|---|
| `README.md` + 22 locale variants | The product's front page. Paths are public URLs. |
| `guides/` (23-locale prompt-cache guide, cost analyses) | Reader-facing publications linked from README. Paths are public URLs. |
| `hooks/session-architecture.md` | **Injected into the user's session by a hook.** Product content that happens to be markdown. |
| `skills/*/SKILL.md`, `skills/*/README.md` | The skills' executable instructions — the LLM is the runtime (see root `CLAUDE.md`). |
| `CLAUDE.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `THIRD-PARTY-NOTICES.md` | Root conventions expected at these exact paths. |

## 3. Release sync surfaces — the must-not-miss list

A behavior change is never one file. When anything user-visible changes, these move **in the same
pass**, and the version-bearing ones must agree **exactly** or the marketplace ships a mismatch:

| # | Surface | Note |
|---|---|---|
| 1 | `.claude-plugin/plugin.json` → `version` | Source of truth for the installed version |
| 2 | `CHANGELOG.md` → new heading | Same version string as #1 |
| 3 | `../marketplace/.claude-plugin/marketplace.json` → this plugin's `version` | **Different repo.** Needs its own commit and push |
| 4 | `README.md` **and all 22 locale variants** | A feature described in one locale must be described in all |
| 5 | `CLAUDE.md` | Only when the change creates a rule a future session must not undo |

**Verification before push** (do not trust that a bump happened):

```bash
grep '"version"' .claude-plugin/plugin.json
head -12 CHANGELOG.md | grep '^## '
grep -A2 'claude-code-token-saver' ../marketplace/.claude-plugin/marketplace.json | grep version
git status --short | grep -c '^ M README'   # expect 23 when README text changed
```

**Both repos push, or neither.** Pushing the plugin without the marketplace leaves users on the old
version; pushing the marketplace first advertises a version that does not exist yet.

## 4. Status-claim verification

Claims about release/deploy state rot silently and are invisible to content diffing. Verify, never
trust the label:

| Claim | Verify with |
|---|---|
| "released as vX" | The marketplace entry's `version` (#3 above) — that is what users resolve. **Not git tags**: tagging stopped at `v2.2.0` while releases continued, so a missing tag proves nothing either way |
| "pushed" | `git log --oneline origin/main..HEAD` — must be empty, in **both** repos |
| "installed / live for the user" | `ls ~/.claude/plugins/cache/ww-w-ai/claude-code-token-saver/` — the installed copy is a snapshot, not a symlink, so a source edit is NOT live until `/plugin` update |

## 5. Editing rule

Edit the **source repo**. `~/.claude/plugins/cache/.../claude-code-token-saver/<version>/` is a
read-only copy overwritten on install — edits there are lost and never reach users.

## 6. vault vs repo

Engineering material (CC internals, transcript formats, platform research) → this repo.
Product/market/competitor research → the www-wiki vault as a summary, not here.
